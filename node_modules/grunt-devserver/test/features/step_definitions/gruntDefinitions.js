var gruntInterface = require('../interface/gruntInterface.js')
  , path = require('path')
  , gruntfilePath = path.resolve('./test/features/support/templates/templateGruntFile.js')

function gruntDefinitions() {
    this.Given(/^I am using grunt$/, function(callback) {
        this.interface = new gruntInterface()
        callback()
    });

    this.When(/^I run grunt$/, function(callback) {
        callback.pending()
    });

    this.When(/^I run grunt with the configuration:$/, function(config, callback) {
        callback.pending()
    });
}

module.exports = gruntDefinitions