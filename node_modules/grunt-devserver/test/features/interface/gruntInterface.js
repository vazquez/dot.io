var grunt = require('grunt')

module.exports = gruntInterface

gruntInterface.prototype.run = run

function gruntInterface() { }

function run(parameters, callback) {
    var opts = { grunt: true
               , args: [ '--gruntfile', 'test/fixtures/Gruntfile-print-text.js', 'print:foo' ]
               }
    grunt.util.spawn(opts, callback)
}